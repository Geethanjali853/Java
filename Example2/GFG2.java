import java.util.*;

public class GFG2 {
    public static void main(String[] args) {
        HashSet<Integer> hs = new HashSet<>(); //object of hashset
        hs.add(1); //Adding the elements
        hs.add(2); //Adding the elements
        hs.add(1); //Adding the elements
        System.out.println("Hashset size: " + hs.size()); //Printing size and elements
        System.out.println("Elements in Hashset: " + hs); //Printing size and elements
    }
}
